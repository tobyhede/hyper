# Copy this file to .env. Choose a URL-safe local password, then use it in both
# POSTGRES_PASSWORD and DATABASE_URL. This template cannot connect as committed.
POSTGRES_DB=hyper
POSTGRES_USER=hyper
POSTGRES_PASSWORD=
POSTGRES_PORT=55432
DATABASE_URL=
