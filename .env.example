# Copy this file to .env, then fill both blank values with the same URL-safe
# local password. This template is intentionally not ready to connect.
POSTGRES_DB=hyper
POSTGRES_USER=hyper
POSTGRES_PASSWORD=
POSTGRES_PORT=55432
DATABASE_URL=
